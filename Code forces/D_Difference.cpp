#include <bits/stdc++.h>

using namespace std;

int main()
{

    long long int x, a, b, c, d;

    cin >> a >> b >> c >> d;
    x = (a * b) - (c * d);
    cout << "Difference = " << x;
    return 0;
}
